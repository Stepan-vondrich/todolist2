================================================================================
  NASAZENÍ APLIKACE TODOLIST2 DO AZURE CONTAINER APPS
  Dokumentace nasazení – krok za krokem, s vysvětlením
================================================================================

Aplikace: fullstack TODO list
  - Frontend: React + TypeScript (build nástrojem Vite)
  - Backend:  C# ASP.NET Core Web API (.NET 8)
  - Data:     SQLite (lokálně) -> Azure SQL -> nakonec Neon Postgres (viz ČÁST C)

Cíl: vzít hotovou aplikaci, zabalit ji do Docker image, nahrát do registru
     a rozběhnout v Azure Container Apps na adrese
     https://app-containerapp-01.salmondune-4d937f62.northeurope.azurecontainerapps.io

Klíčová myšlenka architektury:
  Backend (.NET) servíruje jak API (/api/...), tak i samotný frontend
  (statické soubory z adresáře wwwroot). Takže nepotřebuju dva kontejnery
  ani dva weby – stačí JEDEN kontejner, který poslouchá na jednom portu
  a obsluhuje úplně všechno. To zjednodušuje nasazení.


================================================================================
  ČÁST A – PŮVODNÍ ZADÁNÍ (4 kroky)
================================================================================

  1) Vytvořit Dockerfile pro frontend i backend
  2) Buildnout image
  3) Nahrát image do registru (ideálně Azure Container Registry – ACR)
  4) V Container App vyměnit demo image za moji image

Později přibyl 5. úkol: vyřešit PERZISTENCI DAT (aby data přežila restart).


================================================================================
  KROK 1 – DOCKERFILE (multi-stage build)
================================================================================

Dockerfile je "recept", podle kterého se postaví image (zabalená aplikace).
Použil jsem tzv. MULTI-STAGE build = více fází v jednom souboru. Důvod: ve
výsledné image nechci mít Node.js ani .NET SDK (kompilátory) – ty potřebuju
jen během buildu. Ve finále chci jen běhové prostředí + zkompilovanou appku.
Tím je výsledná image menší a bezpečnější.

Tři fáze:

  FÁZE 1 (node:20-alpine) – postaví frontend
    - zkopíruje package.json + package-lock.json
    - "npm ci" = nainstaluje přesně zamčené verze závislostí
    - zkopíruje zdrojáky frontendu
    - "npm run build" = Vite vyrobí statické soubory do složky dist/
      (HTML, JS, CSS – hotový web)

  FÁZE 2 (dotnet/sdk:8.0) – zkompiluje backend
    - "dotnet restore" = stáhne NuGet balíčky
    - "dotnet publish -c Release" = zkompiluje backend do /app/publish

  FÁZE 3 (dotnet/aspnet:8.0) – běhový obraz (runtime), to co se reálně spustí
    - zkopíruje zkompilovaný backend z FÁZE 2
    - zkopíruje hotový frontend (dist/) z FÁZE 1 do složky wwwroot
    - nastaví, na čem má appka poslouchat, a spustí ji

Důležitá nastavení v běhové fázi:
    ENV ASPNETCORE_URLS=http://+:8080   -> poslouchá na VŠECH síťových
                                            rozhraních (0.0.0.0), port 8080
    EXPOSE 8080
    ENTRYPOINT ["dotnet", "TodoApi.dll"]

  (ZMĚNA OPROTI PŮVODNÍMU NÁVRHU č.1: Lokálně appka běžela na "localhost"
   a portu 6001. V kontejneru ale "localhost" znamená jen vnitřek kontejneru –
   zvenku by se na ni nikdo nedostal. Proto bind na "http://+:8080"
   = naslouchej na všech adresách. Číslo 8080 je běžná konvence pro
   kontejnery a Azure Container Apps.)

  (ZMĚNA č.2: Projekt měl v .csproj nastaveno OutputType=WinExe – tj. build
   pro Windows GUI .exe. To je pro Linux kontejner nesmysl. Vyřešil jsem to
   tím, že kontejner spouštím přes "dotnet TodoApi.dll" (ne přes .exe),
   takže Windows-specifické nastavení nevadí.)

  (ZMĚNA č.3 – úprava kódu Program.cs: Aplikace se po startu pokoušela
   automaticky OTEVŘÍT PROHLÍŽEČ (Process.Start). To dává smysl u desktop
   .exe na Windows, ale v Linux kontejneru žádný prohlížeč není a appka by
   spadla. Obalil jsem to podmínkou "jen na Windows" (OperatingSystem.
   IsWindows()) + try/catch.)

Vytvořil jsem také .dockerignore (obdoba .gitignore) – říká, co se NEMÁ
posílat do buildu: node_modules, bin, obj, databáze, uploads, .git…
Tím je build rychlejší a image čistší.


================================================================================
  KROK 2 – BUILD IMAGE PŘES DOCKER DESKTOP  (na tohle se ptali konkrétně)
================================================================================

Co je Docker Desktop:
  Aplikace na Windows, která v pozadí provozuje Docker engine (démona).
  Na Windows běží přes WSL2  "Linux engine" – tzn. i když sedím na Windows,
  kontejnery se staví a běží jako LINUX kontejnery (což je přesně to, co
  Azure Container Apps chce – linux/amd64).

Jak build probíhal:
  1) Nastartoval jsem Docker Desktop (musí běžet démon, jinak "docker"
     příkazy hlásí, že se nemůžou připojit k engine).
  2) Spustil jsem build z kořene projektu:

        docker build -t todolist2:latest G:\projects\todolist2

     - "docker build"     = postav image podle Dockerfile
     - "-t todolist2:latest" = pojmenuj image (tag) "todolist2", verze "latest"
     - "G:\projects\todolist2" = build context = složka, ze které se berou
                               soubory (a kde leží Dockerfile)

  3) Co se dělo uvnitř (BuildKit – moderní buildovací engine Dockeru):
     - Docker poslal build context (soubory bez toho, co je v .dockerignore)
       do enginu.
     - Postupně provedl všechny tři fáze z Dockerfile.
     - Každý příkaz v Dockerfile = jedna "vrstva" (layer). Vrstvy se kešují,
       takže při dalším buildu, když se zdrojáky nezměnily, použije cache
       a je to bleskové.
     - Multi-stage znamená, že FÁZE 1 a 2 (s kompilátory) se na konci ZAHODÍ
       a do finální image se z nich jen zkopírují výsledky.
  4) Výsledek: image "todolist2:latest", velikost cca 388 MB
     (běhové .NET prostředí + zkompilovaná appka + frontend).
  5) Ověření: "docker images" ukáže hotovou image v lokálním úložišti.

  (ZMĚNA / KOMPLIKACE č.4 – velmi důležitá:
   PŮVODNĚ jsem chtěl image vůbec nestavět lokálně, ale nechat ji postavit
   přímo v cloudu příkazem "az acr build" (build běží na serverech Azure).
   To je elegantní, protože se obejde lokální prostředí. JENŽE Azure to
   odmítlo s chybou "TasksOperationsNotAllowed" – tato (nová/free)
   subscription nemá povolené ACR Tasks (cloudové buildy).
   ŘEŠENÍ: postavil jsem image LOKÁLNĚ přes Docker Desktop a pak ji ručně
   nahrál do registru. Proto se Docker Desktop vůbec dostal ke slovu.)

  (KOMPLIKACE č.5 – AVG antivirus: První lokální build SELHAL na "dotnet
   restore" (nešlo stáhnout balíčky z nuget.org). Příčina: AVG dělá tzv.
   TLS interception – odposlouchává HTTPS a podepisuje ho vlastním
   certifikátem, kterému Linux kontejner nedůvěřuje. Po dočasném vypnutí
   "HTTPS scanning" v AVG build proběhl. Stejný AVG problém blokoval i
   přihlášení do Azure – viz krok 4.)


================================================================================
  KROK 3 – NAHRÁNÍ IMAGE DO REGISTRU (Azure Container Registry – ACR)
================================================================================

Registr = soukromé úložiště Docker images v cloudu (jako "GitHub pro images").
Container App si odtud image stáhne a spustí.

  1) Vytvořil jsem ACR:
        az acr create --name acrtodolist783a4e6d --sku Basic --admin-enabled true
     - SKU Basic = nejlevnější varianta
     - admin-enabled = zapne uživatelské jméno+heslo, kterým se Container App
       k registru přihlásí (jednodušší než spravovaná identita)

  2) Přihlásil jsem Docker do registru:
        az acr login --name acrtodolist783a4e6d

  3) "Otagoval" jsem lokální image plným jménem registru (aby Docker věděl,
     kam ji má poslat):
        docker tag todolist2:latest acrtodolist783a4e6d.azurecr.io/todolist2:latest

  4) Nahrál (push):
        docker push acrtodolist783a4e6d.azurecr.io/todolist2:latest
     Docker nahraje jednotlivé vrstvy; ty, co už v registru jsou, přeskočí.

  (KOMPLIKACE č.6 – musel jsem nejdřív zaregistrovat "resource provider"
   Microsoft.ContainerRegistry. Nová subscription nemá automaticky povolené
   všechny služby – musí se jednorázově "zaregistrovat" příkazem
   "az provider register". Stejné jsem pak dělal i pro Microsoft.Storage
   a Microsoft.Sql.)


================================================================================
  KROK 4 – VÝMĚNA DEMO IMAGE V CONTAINER APP
================================================================================

Container App "app-containerapp-01" původně běžel na demo image
"containerapps-helloworld". Vyměnil jsem ji za moji.

Než to šlo, musel jsem se přihlásit do Azure CLI ("az login"):

  (KOMPLIKACE č.7 – přihlášení dalo zabrat:
   a) AVG opět rozbíjel TLS (popsáno výše) – řešení: vypnout HTTPS scanning.
   b) Klasické "az login" přes okno Windows (WAM broker) se rušilo s chybou
      "User Cancelled" + zablokovaný PIN. Přepnul jsem na DEVICE CODE login:
      "az login --use-device-code" -> vypíše kód, ten zadáš na
      microsoft.com/devicelogin a přihlásíš se heslem. Spolehlivější.
   c) ZMĚNA: účet stepanvondrich@gmail.com NEMĚL žádnou subscription –
      Azure resources jsou pod účtem vondrich.stepan@seznam.cz. Přihlásil
      jsem se tedy tím správným účtem.)

Samotná výměna (tři příkazy):
  1) Nastavit registr + přihlašovací údaje na Container App:
        az containerapp registry set ... (server, username, password)
  2) Vyměnit image:
        az containerapp update --image acrtodolist783a4e6d.azurecr.io/todolist2:latest
  3) Změnit příchozí port (ingress):
        az containerapp ingress update --target-port 8080

  (ZMĚNA č.8: Demo image poslouchala na portu 80, moje appka na 8080.
   Proto jsem musel přenastavit "targetPort" ingressu z 80 na 8080,
   jinak by Container App posílal provoz na špatný port.)

Ověření: nová revize "app-containerapp-01--0000001" naběhla jako
Running/Healthy; GET / vrátil 200 + HTML s <title>Todo List</title>;
GET /api/todos vrátil [] (prázdný seznam). Funguje.

  POZNÁMKA – pojem REVIZE: Container Apps verzují nasazení pomocí "revizí".
  Každá změna (nová image, nová env proměnná, jiný port…) vyrobí novou revizi
  (0000001, 0000002…). Provoz jde na nejnovější zdravou revizi; když nová
  spadne, stará dál obsluhuje – takže appka nikdy "nezhasne".


================================================================================
  KROK 5 – PERZISTENCE DAT (přidaný úkol)  – nejzajímavější část
================================================================================

Problém: kontejner má jen DOČASNÉ úložiště. Když se restartuje nebo nasadí
nová revize, jeho disk se vynuluje. Takže SQLite databáze (todos.db) i
nahrané přílohy by se ZTRATILY. To je pro reálné použití nepřijatelné.

První nápad (a proč NEVYŠEL):
  Připojit do kontejneru perzistentní disk Azure Files (síťové úložiště)
  a dát na něj SQLite databázi i uploads.
  - Připravil jsem to: storage účet sttodolist783a4e6d, file share "tododata",
    namountoval na cestu /data, nasměroval tam appku přes proměnnou DATA_DIR.
  - JENŽE revize SPADLA. V logu: "SQLite Error 5: database is locked".

  VYSVĚTLENÍ (důležité):
  SQLite ke své práci potřebuje zamykání souborů (file locking). Azure Files
  přes protokol SMB tohle zamykání plně nepodporuje. SQLite proto zatuhne a
  spadne. Je to známé omezení – SQLite se na síťové sdílené disky nehodí.
  (Container Apps navíc neumí mount option "nobrl", který to jinde obchází.)

  (ZMĚNA č.9 – ZÁSADNÍ ROZHODNUTÍ: Místo "SQLite na síťovém disku" jsem
   databázi přesunul do plnohodnotné databázové služby AZURE SQL. Soubory
   příloh (uploads) ALE na Azure Files nechat můžou – obyčejné soubory
   zamykání nepotřebují, takže ty přes SMB fungují bez problému.
   Výsledek: rozdělené úložiště = DB v Azure SQL, soubory na Azure Files.)

Co jsem pro to udělal:

  A) ÚPRAVA KÓDU – appka teď umí dva druhy databáze:
     - Lokálně (na mém PC / .exe): zůstává SQLite (jednoduché, bez serveru).
     - V kontejneru: Azure SQL.
     Volba se dělá automaticky podle toho, jestli je nastavená proměnná
     AZURE_SQL_CONNECTION:
        když je nastavená  -> použij Azure SQL (UseSqlServer)
        když není          -> použij SQLite
     (Centralizoval jsem také cesty k datům do nové třídy DataPaths.cs.)

     (ZMĚNA č.10: Schéma databáze. U SQLite appka používala "migrace"
      (EF Core Migrate). Pro Azure SQL jsem zvolil EnsureCreated() –
      vytvoří tabulky rovnou podle aktuálního modelu, bez nutnosti udržovat
      druhou sadu migrací pro jiný typ databáze. Méně práce, čistší.)

  B) AZURE SQL – založení databáze:
     - free serverless varianta (zdarma; "serverless" = uspí se při
       nečinnosti a probudí se při dotazu, platí se jen za běh).
     - connection string jsem uložil jako TAJNÝ KÓD (secret) v Container App
       a do appky ho předal jako proměnnou AZURE_SQL_CONNECTION.
       (Heslo se tak nikde neukazuje v plain textu.)
     - firewall: povolit "Azure services", aby se Container App k DB dostal.

     (KOMPLIKACE č.11: North Europe ANI West Europe zrovna NEPOVOLOVALY
      vytvoření nového SQL serveru (chyba RegionDoesNotAllowProvisioning –
      kapacitní/omezení regionu). Proto databáze nakonec běží ve SWEDEN
      CENTRAL. Adresa serveru je nezávislá na regionu, takže to nevadí;
      latence je zanedbatelná.)

  C) PŘÍLOHY – zůstávají na Azure Files:
     - file share "tododata" namountovaný na /data,
     - proměnná DATA_DIR=/data -> appka ukládá přílohy do /data/uploads,
     - to je perzistentní (přežije restart).

Stav v této fázi (revize 0000004):
  - Databáze:  Azure SQL  (server sqltodo783a4e6d-swedencentral, db tododb)
  - Přílohy:   Azure Files (share tododata -> /data/uploads)
  - Tajný kód: azure-sql-connection (connection string)
  >>> POZDĚJI NAHRAZENO: Azure SQL -> Neon Postgres, ACR -> GHCR, Azure SQL
      i ACR SMAZÁNY (náklad na ~€0). Aktuální finální stav viz ČÁST C níže.

OVĚŘENÍ PERZISTENCE (klíčový test):
  1) Přes API jsem vytvořil testovací TODO.
  2) RESTARTOVAL jsem repliku kontejneru.
  3) Po restartu tam TODO pořád bylo.
  => Důkaz, že data jsou bezpečně v Azure SQL, ne na dočasném disku.
  (Testovací záznam jsem pak uklidil, DB je prázdná.)


================================================================================
  ČÁST C – OPTIMALIZACE NÁKLADŮ A STARTU
           (pozdější přechod: ACR->GHCR, Azure SQL->Neon; náklad ~€5/měs -> ~€0)
================================================================================

Nasazení z ČÁSTI A/B fungovalo, ale mělo dvě slabiny:
  - Jediný reálný NÁKLAD byl Azure Container Registry (ACR) v tieru Basic:
    ~€5/měsíc PAUŠÁLNĚ, jen za to, že registr existuje (ne za používání).
    Vše ostatní (Azure SQL free-limit, Container Apps scale-to-zero, Azure
    Files) vyšlo prakticky na €0.
  - Azure SQL (serverless) se při nečinnosti USPÍ; první požadavek ji musí
    probudit -> pomalejší studený start + otravný "probudit DB před prod
    deployem" krok v pipeline (firewall rule -> SELECT 1 -> schema krok).

Cíl: srazit náklad na ~€0 a zjednodušit provoz, BEZ ztráty funkčnosti.

--------------------------------------------------------------------------------
  C1) REGISTR: ACR -> GitHub Container Registry (GHCR)   [OK]
--------------------------------------------------------------------------------
Registr je jen ÚLOŽIŠTĚ image — appce za běhu nic nedává, takže ho lze
nahradit levnějším bez dopadu na funkčnost.

GHCR (ghcr.io) je registr od GitHubu; pro tento (veřejný) repo je ZDARMA a
integruje se s Actions (push přes vestavěný GITHUB_TOKEN, žádné další údaje).
  - Package jsem nastavil jako PRIVATE. Aby si image Container Apps stáhly,
    potřebují token: classic PAT se scope jen "read:packages" a "No expiration"
    (set-and-forget, žádná rotace; kdyby unikl, jde jedním klikem Revoke).
    Token je uložený jako registry credential na OBOU Container Apps.
  - cd.yml: build se přihlásí do GHCR (GITHUB_TOKEN) a pushne image tam;
    dev i prod pak tahají ghcr.io/stepan-vondrich/todolist2:<sha>.
  - Po ověření, že dev i prod běží z GHCR, jsem ACR SMAZAL (az acr delete)
    -> ten ~€5/měsíc zmizel.

  (POZN.: kdyby image byla PUBLIC, je zdarma úplně bez tokenu — a protože
   zdroják už veřejný je a v image nejsou žádné secrets, bylo by to bezpečné.
   Private jsem zvolil jen kvůli skrytí image; funkčně je to jedno.)
  (TRADE-OFF: GHCR není v mém Azure regionu jako ACR, takže úplně první pull
   při studeném startu je o ~1-2 s pomalejší. Image se na node kešuje -> jinak
   zanedbatelné.)

--------------------------------------------------------------------------------
  C2) DATABÁZE: Azure SQL -> Neon (serverless Postgres, externí)   [OK]
--------------------------------------------------------------------------------
Neon je externí serverless PostgreSQL s free tierem (0,5 GB, scale-to-zero,
probudí se za ~1 s). Vždy dostupný, bez měsíčního limitu hodin.

  - ÚPRAVA KÓDU: appka teď umí i Postgres. Výběr provideru podle env var,
    od nejkonkrétnějšího:
        POSTGRES_CONNECTION  -> Postgres (Neon)   [UseNpgsql]
        AZURE_SQL_CONNECTION -> Azure SQL          [UseSqlServer]
        nic                  -> SQLite (lokální)   [Migrate]
    Přidal jsem NuGet Npgsql.EntityFrameworkCore.PostgreSQL a třídu
    PgConnection.Normalize (převede Neonův "postgres://..." URI na formát pro
    Npgsql; pokrytO xUnit testem — TDD červená/zelená).
  - IZOLACE DEV/PROD: každé prostředí má SVŮJ Neon projekt (vlastní data i
    vlastní free 0,5 GB):
        dev  -> projekt "tododb"       (POSTGRES_CONNECTION na todolist-dev)
        prod -> projekt "tododb-prod"  (POSTGRES_CONNECTION na app-containerapp-01)
    Sdílená je jen IMAGE; DB jsou oddělené, data se nikdy nemíchají.
  - SCHÉMA: Postgres si appka vytvoří sama při startu (EnsureCreated), takže
    ODPADL krok "Apply prod DB schema" (azure/sql-action) v pipeline i celý
    "probudit DB před deployem" tanec.
  - Po ověření jsem Azure SQL SMAZAL (server + db + Container App secret + env
    var + GitHub secret AZURE_SQL_CONNECTION + obsolete db/prod-schema.sql).

  (Přílohy (uploads) ZŮSTÁVAJÍ na Azure Files — soubory zamykání nepotřebují,
   přes SMB fungují dál. Mění se jen databáze.)
  (Data: je to demo, prod se rozjel na PRÁZDNÉ Neon DB; migrace SQL Server ->
   Postgres by byla netriviální a nebyla potřeba.)

--------------------------------------------------------------------------------
  C3) MENŠÍ IMAGE + RYCHLEJŠÍ STUDENÝ START   [OK]
--------------------------------------------------------------------------------
Úprava běhové fáze Dockerfile:
  - aspnet:8.0 -> aspnet:8.0-alpine (~220 MB -> ~110 MB, rychlejší pull).
    Alpine nemá ICU (knihovny národního prostředí) -> doinstaloval jsem
    "icu-libs" + "icu-data-full" (jinak by české datum/formátování spadlo do
    invariant režimu).
  - PublishReadyToRun=true — předkompiluje IL na nativní kód (linux-musl-x64),
    takže start dělá míň JIT kompilace.
  Výsledek: cold start ~27 s -> ~21 s. Zbytek drží aktivace Container App
  (naplánování na node) + probuzení Neonu; na to už image nemá vliv (dál by
  pomohlo jen držet 1 repliku teplou = min-replicas 1, to ale stojí pár korun).

--------------------------------------------------------------------------------
  AKTUÁLNÍ CD PIPELINE (GHCR-only)  — nahrazuje verzi z bodu 6 níže
--------------------------------------------------------------------------------
Build už se NEpřihlašuje do Azure kvůli registru (GHCR jede na GITHUB_TOKEN),
deploy-prod už NEMÁ schema krok (Postgres = EnsureCreated). Zjednodušeně:
  permissions: { packages: write }        # aby GITHUB_TOKEN mohl push na GHCR
  env: GHCR: ghcr.io/stepan-vondrich/todolist2
  build:      docker login ghcr.io (GITHUB_TOKEN) -> docker build+push $GHCR:<sha>
  deploy-dev: az containerapp update -n todolist-dev  --image $GHCR:<sha>   (auto)
  deploy-prod:az containerapp update -n app-containerapp-01 --image $GHCR:<sha>  (approval gate)
Registry creds (PAT) na obou appkách jednorázově:
  az containerapp registry set -n <app> -g rg-containerapp-test-01 \
     --server ghcr.io --username Stepan-vondrich --password <PAT-read:packages-noexpiry>

--------------------------------------------------------------------------------
  FINÁLNÍ STAV PO OPTIMALIZACI  (nahrazuje "finální stav" z KROKU 5 i bodu 7)
--------------------------------------------------------------------------------
  Registr:  GHCR (private, zdarma) — dev i prod;  ACR SMAZÁN
  Databáze: Neon Postgres — dev i prod KAŽDÝ SVŮJ projekt;  Azure SQL SMAZÁN
  Přílohy:  Azure Files (share tododata -> /data/uploads) — beze změny
  Image:    alpine + ReadyToRun (menší, rychlejší start)
  NÁKLAD:   z ~€5/měsíc na PRAKTICKY €0

  Rozdíl dev vs prod = jen env proměnná (POSTGRES_CONNECTION míří na jinou
  Neon DB). Stejná image v obou = "build once, deploy many" (dev testuje
  přesně prod kód).


================================================================================
  KAM SE UKLÁDAJÍ PŘÍLOHY KOMENTÁŘŮ (rychlá odpověď)
================================================================================

  - Samotné soubory: Azure Files share "tododata", složka "uploads"
                     (v kontejneru cesta /data/uploads).
                     V portálu: Účet úložiště sttodolist783a4e6d ->
                     Sdílené složky souborů -> tododata -> uploads.
  - Metadata (cesta, typ, náhled): v databázi (teď Neon Postgres, dřív
                     Azure SQL — viz ČÁST C), tabulka CommentAttachments.
  - Lokálně: složka "uploads" vedle .exe + SQLite todos.db.


================================================================================
  SHRNUTÍ ZMĚN OPROTI PŮVODNÍMU NÁVRHU (rychlý přehled)
================================================================================

  1. localhost:6001  ->  http://+:8080   (kontejner musí poslouchat navenek)
  2. WinExe .exe     ->  spouštění přes "dotnet TodoApi.dll" (Linux)
  3. Auto-otevření prohlížeče omezeno jen na Windows (jinak pád v kontejneru)
  4. Cloudový build (az acr build) NEšel -> build LOKÁLNĚ přes Docker Desktop
  5. AVG TLS interception blokoval build i login -> dočasně vypnout HTTPS scan
  6. Nutná registrace resource providerů (Registry, Storage, Sql)
  7. az login: WAM okno selhalo -> device-code; špatný účet (gmail) -> seznam
  8. Ingress port 80 -> 8080
  9. SQLite na Azure Files SPADNE (locking) -> přesun DB do Azure SQL
 10. EF migrace (SQLite) -> EnsureCreated (Azure SQL)
 11. SQL server nešel v NE/WE -> založen ve Sweden Central


================================================================================
  POJMY, KTERÉ SE MŮŽOU HODIT (slovníček)
================================================================================

  Docker image      = zabalená aplikace + vše potřebné k běhu (read-only šablona)
  Kontejner         = spuštěná instance image
  Dockerfile        = recept, jak image postavit
  Multi-stage build = build ve více fázích; finální image bez kompilátorů
  BuildKit          = moderní buildovací engine Dockeru (cache vrstev)
  Registr (ACR)     = cloudové úložiště images
  Container App     = služba Azure pro běh kontejnerů (bez správy serverů)
  Revize            = verze nasazení Container App
  Ingress           = příchozí HTTP vstup + na jaký port přeposílat (targetPort)
  Volume / mount    = připojení perzistentního úložiště do kontejneru (/data)
  Azure Files (SMB) = síťové souborové úložiště (vhodné pro soubory, ne pro SQLite)
  Azure SQL         = spravovaná SQL databáze (serverless = uspí se při nečinnosti)
  Secret            = tajná hodnota (heslo, connection string) uložená v appce
  Resource provider = služba Azure, kterou je třeba v subscription zaregistrovat
  TLS interception  = antivirus/proxy odposlouchává HTTPS vlastním certifikátem
  GHCR              = GitHub Container Registry (registr od GitHubu; pro veřejný repo zdarma)
  PAT               = Personal Access Token GitHubu (přístupový token; scope read:packages)
  Neon              = externí serverless PostgreSQL (free tier 0,5 GB, scale-to-zero)
  ReadyToRun (R2R)  = předkompilace .NET IL na nativní kód -> rychlejší start (míň JITu)
  ICU               = knihovny národního prostředí (data/formátování); Alpine je nemá defaultně
================================================================================


================================================================================
  PŘÍKAZY PODLE DEVOPS PIPELINE (mapa na zadanou tabulku)
================================================================================

Legenda:
  [OK]    = v tomto nasazení jsem reálně spustil
  [RUČNĚ] = udělal jsem ručně přes CLI (v plné pipeline by to dělal runner sám)
  [N/A]   = v tomhle běhu NEBYLO; uvádím doporučenou podobu pro pipeline

DŮLEŽITÉ PŘIZNÁNÍ: celé nasazení proběhlo RUČNĚ z lokálu (Azure CLI + Docker).
Nebyl zapojen vzdálený git remote ani žádná CI/CD pipeline, dev/prod oddělení
ani approval gate. U těch položek proto píšu, jak by to vypadalo, ale
nespouštěl jsem je.

--------------------------------------------------------------------------------
0) PŘEDPOKLADY – instalace nástrojů + přihlášení   [OK]
--------------------------------------------------------------------------------
# instalace Azure CLI (Windows přes winget)
winget install -e --id Microsoft.AzureCLI --source winget --accept-source-agreements --accept-package-agreements

# přihlášení (okno WAM selhávalo -> přepnuto na device-code flow)
az config set core.enable_broker_on_windows=false
az login --use-device-code
az account show

# jednorázová registrace resource providerů (nová subscription je nemá zaplé)
az provider register --namespace Microsoft.ContainerRegistry
az provider register --namespace Microsoft.Storage
az provider register --namespace Microsoft.Sql
az provider show --namespace Microsoft.ContainerRegistry --query registrationState -o tsv

--------------------------------------------------------------------------------
1) GIT REPOSITORY   (GitHub / Azure Repos)
--------------------------------------------------------------------------------
[OK – NASAZENO] Lokální git + veřejné GitHub repo:
https://github.com/Stepan-vondrich/todolist2 (public)

# lokální commit
git add -A
git commit -m "Containerize + deploy to Azure Container Apps with persistence"

# GitHub CLI – instalace + přihlášení (gh funguje i se zapnutým AVG):
winget install -e --id GitHub.cli --source winget --accept-source-agreements --accept-package-agreements
gh auth login --web --hostname github.com --git-protocol https   # device-code: kód zadáš na github.com/login/device

# vytvoření public repa + remote "origin" + push jedním příkazem:
gh repo create todolist2 --public --source=. --remote=origin --push
# další změny pak stačí:  git push

# (BEZPEČNOST) odstranění citlivé hodnoty (subscription ID) i z HISTORIE commitů:
git filter-branch --force --tree-filter \
  'sed -i "s/<staré-subscription-id>/<SUBSCRIPTION_ID>/g" azure-nasazeni.txt' -- --all
git for-each-ref --format="%(refname)" refs/original/ | xargs -n1 git update-ref -d   # smazat zálohy
git reflog expire --expire=now --all && git gc --prune=now                           # pročistit
git push --force origin master                                                        # přepsat historii i na GitHubu
# POZN.: HEAD scrub nestačí – stará hodnota zůstává v historii, dokud se nepřepíše.

[N/A] Alternativa Azure Repos (nepoužito):
az repos create --name todolist2 --project <project> --organization https://dev.azure.com/<org>
git remote add origin https://dev.azure.com/<org>/<project>/_git/todolist2
git push -u origin master

--------------------------------------------------------------------------------
2) CI PIPELINE   (build React + .NET backend)
--------------------------------------------------------------------------------
[RUČNĚ] Reálně jsem build dělal lokálně – buď přímo, nebo přes multi-stage
Docker (bod 3). Holé build příkazy:
# frontend
npm ci
npm run build                 # tsc && vite build  -> dist/
# backend
dotnet restore
dotnet publish backend/TodoApi/TodoApi.csproj -c Release -o publish
# vše dohromady (lokální all-in-one skript)
.\build.ps1

[N/A] Jako CI (GitHub Actions, .github/workflows/ci.yml):
name: CI
on: { push: { branches: [ master ] } }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
        working-directory: frontend
      - run: npm run build
        working-directory: frontend
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: 8.0.x }
      - run: dotnet test backend/TodoApi.Tests/TodoApi.Tests.csproj    # xUnit testy
      - run: dotnet publish backend/TodoApi/TodoApi.csproj -c Release

--------------------------------------------------------------------------------
3) DOCKERFILE   (backend Dockerfile; varianta frontend přes nginx)
--------------------------------------------------------------------------------
[OK] Reálně: JEDEN multi-stage Dockerfile – backend (.NET) servíruje i frontend
z /wwwroot. nginx jsem NEpoužil (jeden kontejner, jeden port 8080).
# soubor Dockerfile v rootu repa (viz KROK 1 výše), build:
docker build -t todolist2:latest .

[N/A] Varianta se SAMOSTATNÝM frontendem přes nginx (kdyby běžel zvlášť):
# frontend/Dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
# + nginx.conf s proxy /api -> backend

--------------------------------------------------------------------------------
4) REGISTRY   (Azure Container Registry)   [OK]
--------------------------------------------------------------------------------
az acr check-name --name acrtodolist783a4e6d
az acr create --resource-group rg-containerapp-test-01 --name acrtodolist783a4e6d \
  --sku Basic --admin-enabled true --location northeurope
az acr login --name acrtodolist783a4e6d
az acr credential show --name acrtodolist783a4e6d        # username + heslo pro pull

--------------------------------------------------------------------------------
5) PUBLIKACE ARTEFAKTU   (image push do ACR)   [OK]
--------------------------------------------------------------------------------
docker tag todolist2:latest acrtodolist783a4e6d.azurecr.io/todolist2:latest
docker tag todolist2:latest acrtodolist783a4e6d.azurecr.io/todolist2:v20260622-230108
docker push acrtodolist783a4e6d.azurecr.io/todolist2:v20260622-230108
docker push acrtodolist783a4e6d.azurecr.io/todolist2:latest

# POZN.: nejdřív jsem zkusil cloud build přímo v ACR, ale subscription to zakazuje:
#   az acr build --registry acrtodolist783a4e6d --image todolist2:latest .
#   -> ERROR: TasksOperationsNotAllowed   => proto build LOKÁLNĚ + ruční push.

--------------------------------------------------------------------------------
6) CD PIPELINE   (deploy do Azure Container Apps)
--------------------------------------------------------------------------------
[OK] Reálné příkazy (ruční CD):
# zjištění existující appky + resource group
az containerapp list -o table
# přihlašovací údaje k registru (aby si app stáhla image)
az containerapp registry set -n app-containerapp-01 -g rg-containerapp-test-01 \
  --server acrtodolist783a4e6d.azurecr.io --username <u> --password <p>
# výměna image -> vznikne nová revize
az containerapp update -n app-containerapp-01 -g rg-containerapp-test-01 \
  --image acrtodolist783a4e6d.azurecr.io/todolist2:v20260622-230108
# port ingressu (demo image měla 80, moje appka 8080)
az containerapp ingress update -n app-containerapp-01 -g rg-containerapp-test-01 --target-port 8080
# složitější změny (volumes, env, secretRef) jdou jen přes YAML:
az containerapp show -n app-containerapp-01 -g rg-containerapp-test-01 -o yaml > app.yaml
#   ... úprava app.yaml: env (AZURE_SQL_CONNECTION secretRef, DATA_DIR), volumeMounts, volumes ...
az containerapp update -n app-containerapp-01 -g rg-containerapp-test-01 --yaml app.yaml
# kontrola / ladění běhu:
az containerapp revision list -n app-containerapp-01 -g rg-containerapp-test-01 -o table
az containerapp logs show -n app-containerapp-01 -g rg-containerapp-test-01 --tail 40
az containerapp revision restart -n app-containerapp-01 -g rg-containerapp-test-01 --revision <rev>

[OK – NASAZENO] Reálná CD pipeline: .github/workflows/cd.yml (commitnuto, běží na push)
Co pokrývá:  GIT      = spouštěč (push do repa Stepan-vondrich/todolist2)
             AZURE    = build -> push do ACR -> nová revize Container App
             LOCALHOST= NELZE. GitHub runner běží v cloudu a na tvůj počítač
                        (:5000/:6001) nevidí. Localhost se dělá lokálně přes
                        build.ps1, nebo by to vyžadovalo self-hosted runner na PC.
Tři joby: build -> deploy-dev (AUTOMATICKY, bez gate) -> deploy-prod
(environment: production = APPROVAL GATE, čeká na ruční odkliknutí na GitHubu).
Dev = Container App "todolist-dev" (SQLite ephemeral, zdarma), stejná RG i ACR.

# .github/workflows/cd.yml
name: CD - build & deploy to Azure
on:
  push:
    branches: [ master ]        # GIT: spustí se na každý push do master
  workflow_dispatch: {}         # + ruční spuštění tlačítkem na GitHubu
env:
  ACR: acrtodolist783a4e6d.azurecr.io
  IMAGE: todolist2
  RG: rg-containerapp-test-01
  APP: app-containerapp-01     # prod
  APP_DEV: todolist-dev        # dev (SQLite ephemeral)
jobs:
  build:                                             # AZURE: artefakt do ACR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with: { creds: ${{ secrets.AZURE_CREDENTIALS }} }
      - run: az acr login --name acrtodolist783a4e6d
      - run: |
          docker build -t "$ACR/$IMAGE:${{ github.sha }}" -t "$ACR/$IMAGE:latest" .
          docker push "$ACR/$IMAGE:${{ github.sha }}"
          docker push "$ACR/$IMAGE:latest"
  deploy-dev:                                        # AZURE: nasadí se AUTOMATICKY (bez gate)
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: azure/login@v2
        with: { creds: ${{ secrets.AZURE_CREDENTIALS }} }
      - run: az extension add --name containerapp --only-show-errors
      - run: az containerapp update -n "$APP_DEV" -g "$RG" --image "$ACR/$IMAGE:${{ github.sha }}"
  deploy-prod:                                       # AZURE: až po dev a po schválení
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: production                          # <- APPROVAL GATE (ruční schválení)
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with: { creds: ${{ secrets.AZURE_CREDENTIALS }} }
      # PROD DB schéma: EnsureCreated nemigruje, takže nové sloupce přidává tento
      # idempotentní skript (db/prod-schema.sql) PŘED nasazením image.
      - uses: azure/sql-action@v2
        with:
          connection-string: ${{ secrets.AZURE_SQL_CONNECTION }}
          path: ./db/prod-schema.sql
      - run: az extension add --name containerapp --only-show-errors
      - run: az containerapp update -n "$APP" -g "$RG" --image "$ACR/$IMAGE:${{ github.sha }}"

# db/prod-schema.sql = idempotentní ALTER TABLE (guard přes COL_LENGTH), spouští se
# v deploy-prod jobu při každém produkčním nasazení. Nové sloupce se jen přidávají
# na konec skriptu. Tím se řeší, že prod (Azure SQL/EnsureCreated) sám nemigruje.

# JEDNORÁZOVÉ NASTAVENÍ – reálně provedené příkazy [OK]:
# 1) Service principal (contributor na resource group) -> rovnou do GitHub secretu:
az ad sp create-for-rbac --name todolist2-cd --role contributor \
  --scopes /subscriptions/<SUBSCRIPTION_ID>/resourceGroups/rg-containerapp-test-01 \
  --json-auth | gh secret set AZURE_CREDENTIALS --repo Stepan-vondrich/todolist2
# 2) právo pushovat image do ACR (jinak by build job selhal na push):
az role assignment create --assignee <clientId-SP> --role AcrPush \
  --scope $(az acr show -n acrtodolist783a4e6d --query id -o tsv)
# 3) GitHub Environment "production" s povinným schvalovatelem (= approval gate),
#    viz bod 8.
# POZN.: az kroky vyžadují vypnutý AVG HTTPS scanning (jinak SSL chyba); gh funguje i s AVG.

--------------------------------------------------------------------------------
7) PROSTŘEDÍ   (dev/prod jako dvě Container Apps nebo dvě resource groups)
--------------------------------------------------------------------------------
[OK – NASAZENO] Dvě Container Apps ve SPOLEČNÉ RG i environmentu (nejlevnější
varianta; SP má práva na celou RG, takže pipeline spravuje obě):
  PROD: app-containerapp-01  -> Azure SQL + Azure Files (perzistentní)
  DEV:  todolist-dev         -> SQLite ephemeral (bez SQL conn / volume = ZDARMA),
        https://todolist-dev.salmondune-4d937f62.northeurope.azurecontainerapps.io

Reálně provedený příkaz (vytvoření dev appky; vyžaduje AVG HTTPS scan vyplý):
u=$(az acr credential show -n acrtodolist783a4e6d --query username -o tsv)
p=$(az acr credential show -n acrtodolist783a4e6d --query passwords[0].value -o tsv)
az containerapp create -n todolist-dev -g rg-containerapp-test-01 \
  --environment env-containerapp-ne-01 \
  --image acrtodolist783a4e6d.azurecr.io/todolist2:latest \
  --target-port 8080 --ingress external \
  --registry-server acrtodolist783a4e6d.azurecr.io --registry-username "$u" --registry-password "$p" \
  --min-replicas 0 --max-replicas 1
# dev běží bez AZURE_SQL_CONNECTION -> appka spadne na SQLite (ephemeral) = bez nákladů.
# pipeline pak dev jen update-uje (job deploy-dev, viz bod 6).

[N/A] Tvrdší oddělení (kdyby bylo třeba): každé prostředí ve VLASTNÍ resource group
(rg-todolist-dev / rg-todolist-prod), vlastní environment, sdílené ACR; SP by pak
potřeboval práva na obě RG. Pro tenhle projekt zbytečné.

--------------------------------------------------------------------------------
8) GATES   (manual approval před prod deploy)
--------------------------------------------------------------------------------
[OK – NASAZENO] Approval gate je živý: GitHub Environment "production" s povinným
schvalovatelem = vlastník účtu. Před deploy jobem GitHub čeká na ruční odkliknutí
("Review deployments" -> Approve) v záložce Actions / na detailu běhu.

Reálně provedené příkazy (gh funguje i s AVG zapnutým):
# vytvoření environmentu "production" s povinným reviewerem (já = user id):
UID=$(gh api user --jq .id)
echo '{"reviewers":[{"type":"User","id":'$UID'}]}' | \
  gh api -X PUT repos/Stepan-vondrich/todolist2/environments/production --input -

# v cd.yml se gate aktivuje jediným řádkem v deploy jobu:
#     environment: production
# -> build proběhne hned, deploy se POZASTAVÍ a čeká na schválení.

Azure DevOps ekvivalent: "Approvals and checks" na Environmentu "prod" před deploy stagí.

--------------------------------------------------------------------------------
9) CLOUD DEPLOYMENT (Azure) – PODPŮRNÉ ZDROJE (perzistence)   [OK]
--------------------------------------------------------------------------------
# Databáze – Azure SQL (free serverless); NE/WE region odmítal -> Sweden Central:
az sql server create -n sqltodo783a4e6d-swedencentral -g rg-containerapp-test-01 \
  -l swedencentral --admin-user sqladmin --admin-password <PW>
az sql server firewall-rule create -g rg-containerapp-test-01 -s sqltodo783a4e6d-swedencentral \
  -n AllowAzureServices --start-ip-address 0.0.0.0 --end-ip-address 0.0.0.0
az sql db create -g rg-containerapp-test-01 -s sqltodo783a4e6d-swedencentral -n tododb \
  --edition GeneralPurpose --compute-model Serverless --family Gen5 --capacity 1 \
  --use-free-limit --free-limit-exhaustion-behavior AutoPause --backup-storage-redundancy Local

# connection string uložen jako SECRET v Container App (heslo se nikde neukáže):
az containerapp secret set -n app-containerapp-01 -g rg-containerapp-test-01 \
  --secrets azure-sql-connection="Server=tcp:sqltodo783a4e6d-swedencentral.database.windows.net,1433;Database=tododb;User ID=sqladmin;Password=<PW>;Encrypt=True;"
# env AZURE_SQL_CONNECTION se secretRef se pak nastaví v YAML (bod 6)

# Přílohy – Azure Files (perzistentní soubory; SQLite tam kvůli zámkům nešlo):
az storage account create -n sttodolist783a4e6d -g rg-containerapp-test-01 \
  -l northeurope --sku Standard_LRS --kind StorageV2
az storage account keys list --account-name sttodolist783a4e6d -g rg-containerapp-test-01
az storage share-rm create -g rg-containerapp-test-01 --storage-account sttodolist783a4e6d \
  --name tododata --quota 5
az containerapp env storage set -n env-containerapp-ne-01 -g rg-containerapp-test-01 \
  --storage-name tododata --azure-file-account-name sttodolist783a4e6d \
  --azure-file-account-key <KEY> --azure-file-share-name tododata --access-mode ReadWrite
# volume + mount na /data se nastaví v YAML (bod 6)

================================================================================
  SHRNUTÍ: STAV NASAZENÍ (ruční start -> plná CI/CD)
================================================================================
RUČNĚ jednou (počáteční infra): npm/dotnet/docker build, az acr create, docker push,
            az containerapp create/update/ingress/yaml, az sql + storage, service principal.
AUTOMATIZOVÁNO (hotovo):  GitHub repo + push; CD pipeline (cd.yml) na každý push do master:
            build -> push do GHCR -> deploy DEV (automaticky) -> deploy PROD (manual
            approval gate, čeká na odkliknutí).   [dřív ACR, viz ČÁST C]
PROSTŘEDÍ:  DEV  = todolist-dev (Neon Postgres) – sem se nasazuje automaticky;
            PROD = app-containerapp-01 (Neon Postgres + Azure Files) – jen po schválení.
            (Každé prostředí má SVŮJ Neon projekt. Dřív dev=SQLite ephemeral,
             prod=Azure SQL — nahrazeno, viz ČÁST C. ACR i Azure SQL SMAZÁNY.)
MOŽNÉ DÁL:  CI s automatickými testy na pushi (dotnet test / vitest),
            varianta Azure DevOps, otužení (startup probe / min-replicas pro instant start).
================================================================================


================================================================================
  BĚŽNÝ POSTUP – CO DĚLÁM RUČNĚ (zbytek řeší pipeline)
================================================================================
Pipeline (cd.yml) na každý push automaticky: postaví image -> pushne do ACR ->
nasadí na DEV. Ručně tedy dělám jen tohle:

  1) Upravím kód a ověřím lokálně:
       - rychle: dev server (npm run dev) na http://localhost:6173
       - nebo jako produkční build: .\build.ps1 a spuštěný exe na http://localhost:6001

  2) Commitnu a pushnu:
       git add -A
       git commit -m "popis změny"
       git push
     -> tím se SPUSTÍ pipeline (build + push do ACR + deploy na DEV proběhnou samy).

  3) Otestuji na DEV (po doběhnutí jobu deploy-dev):
       https://todolist-dev.salmondune-4d937f62.northeurope.azurecontainerapps.io
     (první načtení po nečinnosti pár sekund – DEV škáluje na 0)

  4) Když je DEV OK, schválím PROD deploy (approval gate):
       GitHub -> Actions -> daný běh -> "Review deployments" -> production -> Approve
     -> teprve pak se stejná image nasadí na PROD (app-containerapp-01, Azure SQL).

POZN.: build / push do ACR / deploy na DEV NEDĚLÁM ručně – jsou automatické.
Ruční Docker build má smysl jen pro test mimo git (docker build + docker run -p 8080:8080).
================================================================================
